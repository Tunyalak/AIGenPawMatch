# VS Code / Cursor Suggested Settings (FYI)

- Editor: formatOnSave = true
- TypeScript: strict = true
- Angular Language Service enabled
- Editor: renderWhitespace = selection
- Files: eol = LF

Keep project-specific settings in `.vscode/settings.json` if you need to enforce them.