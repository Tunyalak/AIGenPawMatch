# Accessibility & i18n

- Every interactive element must have a **name** (text/aria-label).
- Use semantic HTML (`button`, `nav`, `main`, etc.).
- Manage focus on route/dialog changes.
- Provide **ARIA live regions** for async status when user needs feedback.
- Strings externalized for i18n if the app targets multiple locales.